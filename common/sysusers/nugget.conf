# lava-chicken-os resident agent identity (baked → /usr/lib/sysusers.d/ on Bazzite).
# systemd-sysusers allocates these into persistent /etc at boot with a LOCKED
# password (key-only, no secret) and does NOT create $HOME — first-boot does that.
# Type Name        ID       GECOS                        Home             Shell
g     nugget       970
u     nugget       970:970  "Lava Chicken Nugget agent"  /var/home/nugget /bin/bash
g     nugget-tui   971
